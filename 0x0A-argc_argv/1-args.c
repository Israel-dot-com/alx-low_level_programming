#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 *
 * @argc: Argument Counter
 *
 * @argv: Argument Vector
 *
 * Return: Always (0)
 */

int main(int argc, char *argv[])
{
	if (argc >= 1)
		printf("%d\n", argc);

	return (0);
}
