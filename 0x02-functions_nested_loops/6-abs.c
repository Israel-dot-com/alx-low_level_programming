#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @int: holds the input to be checked
 *
 * Return: Always (0)
 */

int _abs(int)
{
	int r;

	if (r < 0)
	{
		_putchar('-');
		_putchar('\n');
	}
	else
	{
		_putchar('\n');
	}
}
