#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @r: holds the input to be checked
 *
 * Return: Always (0)
 */

int _abs(int r)
{
	int r;

	if (r < 0)
	{
		_putchar('-');
		_putchar('\n');
	}
	else
	{
		_putchar('\n');
	}
}
